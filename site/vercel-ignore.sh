#!/usr/bin/env bash
# stable has no marketing site content. Always skip dayspring-site builds.
# When master (full site/) merges into stable, this file is replaced by the
# real diff-based ignore from master.
exit 0
