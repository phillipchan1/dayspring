# Dependencies
node_modules
.pnp
.pnp.js

# Tauri build output + updater signing keys (NEVER commit the private key)
src-tauri/target
src-tauri/gen/schemas
*.key
*.key.pub

# Build output
dist
dist-ssr
*.local

# Env / secrets — never commit real keys
.env
.env.local
.env.*.local

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Vite / TS
*.tsbuildinfo
.vercel

# Local script caches (SSA names dataset for scripts/demographics.ts)
.cache
.env*.local

# Generated in CI and bundled into the desktop build (not committed)
public/changelog.json
RELEASE_NOTES.md
